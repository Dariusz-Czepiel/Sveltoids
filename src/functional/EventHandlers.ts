import type { CollidableGameObjectTypes } from "./Types";

//add support for context
export const DispatchEvent = (event: ReturnType<CollidableGameObjectTypes['onCollision']>) => {
    if(event != undefined)
    {

    }
}
